# Finance Hero local release sign-off

## Release scope

- Application commit: `d6284cc8e6397c1f6f373b0c3f4359f8d6f593e1`
- Validation date: 2026-08-03 (Asia/Kolkata)
- Release target: local Mac application and trusted-LAN iPhone PWA
- Gmail discovery and sync are parked and are not part of this release.

## Required evidence

| Check | Result | Evidence |
| --- | --- | --- |
| Local diagnostics | Pass | `pnpm doctor:local` passed 10/10 checks. The encrypted database opened successfully, data permissions were `700`, the TLS private key was `600`, and API/web health checks passed. |
| Encrypted backup | Pass | 46 encrypted backups were available and the latest backup passed verification. |
| Full validation | Pass | `pnpm check` passed lint, typecheck, 169 automated tests, production builds, and PWA generation. |
| Desktop runtime | Pass | The encrypted API and HTTPS web app were healthy on ports `4317` and `4318`. |
| Phone HTTPS | Pass | `https://192.168.1.4:4318` served the PWA over the trusted-LAN certificate; `/manifest.webmanifest` and `/sw.js` returned HTTP 200, and `/api/v1/health` reported an encrypted healthy database. |
| Phone layout | Pass | A `390x844` mobile viewport had no horizontal overflow. The Accounts route retained its hash, month, and year after reload. |
| Phone navigation/editing | Pass | Accounts and Expenses loaded, August 2026 was selected, and the expense register exposed 61 editable inputs plus the Save sheet action. |
| Physical iPhone setup | Pass | The local CA profile was installed and trusted, and the user confirmed the PWA setup flow on the iPhone. |

## Notes

- No financial records were changed during release validation.
- The app remains local-only and is available to the phone only while the Mac and Finance Hero local services are running on the same trusted network.
- The production build reported only the existing non-blocking JavaScript chunk-size warning.

## Decision

Release accepted for local Mac and iPhone use. Gmail work remains explicitly deferred.
