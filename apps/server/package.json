{
  "name": "@finance-hero/server",
  "version": "0.1.0",
  "scripts": {
    "test": "vitest run",
    "dev": "tsx watch src/index.ts",
    "build": "tsup src/index.ts --format esm --sourcemap --clean",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "private": true,
  "type": "module",
  "main": "./dist/index.js",
  "dependencies": {
    "@finance-hero/contracts": "workspace:*",
    "@finance-hero/database": "workspace:*",
    "@finance-hero/domain": "workspace:*",
    "fastify": "^5.10.0"
  }
}
